import { motion } from "framer-motion";
import styled from 'styled-components';

export const ContainerCards = styled(motion.div)`
  position: relative;
  display: flex;
  gap: 1.4rem;
  padding: 2.5rem 2.1rem 4rem;
  width: 100%;
  height: 100%;
`